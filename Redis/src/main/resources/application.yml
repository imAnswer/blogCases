spring:
  data:
    redis:
      host: 192.168.64.132
      port: 6379
      ssl:
        enabled: false
  datasource:
    driver: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.64.132:3306/nfturbo?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: lsy152432
  redis:
    redisson:
      config: |
        singleServerConfig:
          idleConnectionTimeout: 10000
          connectTimeout: 10000
          timeout: 3000
          retryAttempts: 3
          retryInterval: 1500
          # 无密码请明确写成 null 或直接删掉 password 行
          password: null
          subscriptionsPerConnection: 5
          clientName: null
          # 👇 地址改成你提供的 IP + 端口，并使用 redis:// 协议（非 rediss://）
          address: "redis://192.168.64.132:6379"
          subscriptionConnectionMinimumIdleSize: 1
          subscriptionConnectionPoolSize: 50
          connectionMinimumIdleSize: 24
          connectionPoolSize: 64
          database: 0
          dnsMonitoringInterval: 5000
        threads: 16
        nettyThreads: 32
        codec: !<org.redisson.client.codec.StringCodec> {}
        transportMode: "NIO"


jetcache:
  statIntervalMinutes: 1       # 统计输出间隔，方便调试，可选
  areaInCacheName: false       # 是否在 key 里包含 area 名，一般关掉即可
  local:
    default:
      type: caffeine           # 本地缓存用 Caffeine
      keyConvertor: fastjson2
  remote:
    default:
      type: redisson      # 直接用 redisson 客户端
      uri: redis://192.168.64.132:6379
      keyConvertor: fastjson2
      valueEncoder: java
      valueDecoder: java
